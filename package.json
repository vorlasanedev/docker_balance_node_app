{
  "name": "docker_balance_node_app",
  "version": "1.0.0",
  "description": "docker load balance with nginx reverse proxy under docker",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2"
  }
}
